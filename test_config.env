# OpenPoke Cache Testing Configuration
# ====================================

# Server Configuration
SERVER_URL="http://localhost:8000"
WEB_URL="http://localhost:3000"

# Test Parameters
DEFAULT_ITERATIONS=10
DEFAULT_CONCURRENT_USERS=5
DEFAULT_TIMEOUT_SECONDS=30

# Cache Configuration for Testing
CACHE_ENABLED_MB=512
CACHE_ENABLED_ENTRIES=100
CACHE_DISABLED_MB=0
CACHE_DISABLED_ENTRIES=0

# Test Messages
TEST_MESSAGES=(
    "Hello, how are you?"
    "What did we just discuss?"
    "Can you summarize our conversation?"
    "Show me the previous messages"
    "What was the last thing you said?"
    "Tell me about our chat history"
)

# Performance Thresholds
EXPECTED_CACHE_HIT_RATE_MIN=80.0
EXPECTED_RESPONSE_TIME_MAX_MS=50.0
EXPECTED_MEMORY_USAGE_MAX_MB=600.0

# Test Scenarios
SCENARIOS=(
    "conversation_history"
    "concurrent_requests"
    "cache_invalidation"
    "memory_limits"
    "error_handling"
)

# Output Configuration
OUTPUT_DIR="./test_results"
LOG_FILE="cache_test.log"
RESULTS_FILE="performance_results.json"
